# macOS
.DS_Store

# Xcode
DerivedData/
build/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# XcodeGen generates the project from project.yml.
# Regenerate anytime with `xcodegen generate`.
*.xcodeproj

# API 키 — 절대 커밋 금지 (프로덕션은 프록시 경유)
TrumanSee/Secrets.swift
